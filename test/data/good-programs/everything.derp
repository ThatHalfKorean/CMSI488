~o< I AM A CROC
nom x derp
nom y derp

dile 4 - x <= 10 && 0 == 1 dur
  nom x derp
  dile false && 1>2 && 1<3 || false!=(1>=100) dur
    buul x derp
    x = x==x && not (y > 3) || x;
    pront y * (3 + y);
  urp
  ~o< I attack when you least expect it. HAhah!!
  x = 1 + 2 - 3;
urp

buul test derp ~o< ok
test = false && 8 != 2 || test && !test;

pront y;
pront y * (3 + 44), 6 - -8;
