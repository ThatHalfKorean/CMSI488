~o< I AM A CROC
nom x derp
nom y derp

eef 1 < 2 dur
  x-- derp ~o< why is this undefined?
urp

~o< elsheef doesn't work
~o< neither does elsh

~o< tri ketch does not work either

dile 4 - x <= 10 && 0 == 1 dur
  nom x derp
  dile false && 1>2 && 1<3 || false!=(1>=100) dur
    nom x derp
    x = x==x && !(y > 3) || x derp
    pront y * (3 + y) derp
  urp
  ~o< I attack when you least expect it. HAhah!!
  x = 1 + 2 - 3 derp
urp

nom test derp ~o< Nurr-hurr strikes again
test = foos && 8 != 2 || test && !test derp

pront y derp
pront y * (3 + 44), 6 - -8 derp
